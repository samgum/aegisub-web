// Layout metrics shared by the cue-list virtualization and the injected CSS, and the
// reading-speed thresholds used for the CPS warnings.
export const ROW_H = 20; // Aegisub default grid font is 9pt; line height is glyph height + 4px
export const OVERSCAN = 6; // extra rows rendered above/below the viewport
export const CPS_WARN = 21;
export const CPS_BAD = 27;
